# ═══════════════════════════════════════════════════════════════════════════════
# Memory System — Dependencies
# ═══════════════════════════════════════════════════════════════════════════════
# Install: pip install -r memory/requirements.txt
#
# Post-install (one-time, downloads NLP models):
#   python -m spacy download en_core_web_sm
#   python -m coreferee install en
#
# Required environment variables:
#   MEMORY_USER_ID    — identity of the memory owner
#   NEO4J_PASSWORD    — Neo4j database password
# ═══════════════════════════════════════════════════════════════════════════════


# ── Long-Term Memory (Neo4j graph) ────────────────────────────────────────────
neo4j>=5.15.0
tenacity>=8.2.3              # connection retry with exponential backoff


# ── Data Models ───────────────────────────────────────────────────────────────
pydantic>=2.5.0              # node + edge schema validation (uses v1 compat layer)


# ── Embeddings + Reranking (local, no API) ────────────────────────────────────
sentence-transformers>=2.2.2 # MiniLM-L6-v2 (384-dim) + ms-marco cross-encoder
numpy>=1.24.3                # required transitively by sentence-transformers


# ── Entity Extraction Pipeline ────────────────────────────────────────────────
spacy>=3.7.0                 # NER + noun-chunk extraction
coreferee>=1.4.0             # pronoun resolution ("she", "him", "that")
gliner>=0.2.0                # zero-shot NER for concept-level entities


# ── Temporal Parsing ──────────────────────────────────────────────────────────
dateparser>=1.2.0            # "last Tuesday", "two weeks ago" → datetime


# ── Consolidation Pipeline ────────────────────────────────────────────────────
# The nightly consolidation pipeline uses the Gemini CLI binary (external tool,
# not a Python package). Install it separately:
#   https://github.com/google-gemini/gemini-cli
# Set GEMINI_API_KEY in your environment before running consolidation.
